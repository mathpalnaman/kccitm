#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,s;
    scanf("%d", &n);
    s=0;
    while(n)
    {
        s+=n%10;
        n/=10;
    }
    printf("%d\n",s);
    return 0;
}
