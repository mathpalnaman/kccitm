#include <bits/stdc++.h>
using namespace std;


int main() {
    int sum,a,b,c;
    cin>>a;
    cin>>b;
    cin>>c;
    if(1<=a<=b<=c<=100)
    {
        sum=a+b+c;
    }
    cout<<sum<<endl;
    return 0;
}
